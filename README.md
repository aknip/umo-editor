# umo-editor

## Kilo Code: Use codebase indexing 
```bash
cd kilo-qdrant-index
docker-compose up
```


```bash
cd umo-editor-source && npm install && npm run build
```
you can run `npm run dev` to start the editor


```bash
cd demo-app && npm install
npm run dev
```